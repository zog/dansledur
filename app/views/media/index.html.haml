= form_tag media_path, method: :get do
	= text_field_tag :search, params[:search]
	= submit_tag :"Get it!", name: nil

%ul
	- @medias.each do |m|
		%li
			= m.name
			%br
			= link_to image_tag(m.image.url(:thumb)), medium_path(m)
			
= link_to "ADD", new_medium_path